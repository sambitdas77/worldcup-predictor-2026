# WC2026 Predictor Frontend

React/Vite frontend for the World Cup prediction model.

```bash
npm install
npm run dev
```

The app currently reads static model output from `src/data/worldcupData.json`.
